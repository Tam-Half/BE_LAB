// const crypto = require('crypto');

// const ACCESS_TOKEN_SECRET = crypto.randomBytes(64).toString('hex');
// const REFRESH_TOKEN_SECRET = crypto.randomBytes(64).toString('hex');

// console.log('[JWT] Pod khởi động với secret mới - session cũ sẽ mất hiệu lực');

// module.exports = { ACCESS_TOKEN_SECRET, REFRESH_TOKEN_SECRET };